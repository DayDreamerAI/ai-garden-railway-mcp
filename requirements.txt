# Core dependencies
neo4j>=5.23.0
aiohttp>=3.9.0
python-dotenv

# JinaV3 Embeddings Support
transformers>=4.31.0
torch>=2.0.0
sentence-transformers>=2.2.0
numpy>=1.24.0
psutil>=5.9.0
einops>=0.7.0

# Optional: Use --no-deps if torch installation fails on Railway
# Railway will automatically select CPU-only torch
